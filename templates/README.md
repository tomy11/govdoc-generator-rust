# Templates

Place bundled `.docx` templates here when the renderer boundary is implemented.

For the first MVP, the app can either:

- call the existing Python `docxtpl` renderer as a sidecar, or
- use a limited native Rust renderer for simple placeholder replacement.

